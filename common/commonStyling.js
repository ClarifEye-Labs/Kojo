import { colors } from "../constants";

const commonStyling = {
  mainContainer: {
    width: '100%',
    height: '100%',
    backgroundColor : colors.offWhite
  },
  backButtonStyling:{
    position: 'absolute',
    top: 40,
    left: 25
  }
}

export default commonStyling